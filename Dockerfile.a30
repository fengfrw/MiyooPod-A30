FROM debian:bullseye

# Add armhf as a foreign architecture for cross-compilation
RUN dpkg --add-architecture armhf

RUN apt-get update && apt-get install -y \
    gcc-arm-linux-gnueabihf \
    libsdl2-dev:armhf \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /build
